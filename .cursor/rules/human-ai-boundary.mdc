---
alwaysApply: true
---
# 🤝 人机协作边界 (Human-AI Boundary)
在进行任何计划 (Plan) 或代码开发时，必须严格遵守以下人机分工原则，以保护游戏引擎的序列化资产安全：

1. **绝对职责隔离**：
   - **Agent 职责**：仅负责脚本代码（C#/TypeScript 等）的编写、核心逻辑实现与终端编译验证。严禁尝试直接修改场景文件、预制体或任何引擎自动生成的 meta/配置资产。
   - **User 职责**：负责所有游戏引擎编辑器内的可视化操作（如修改 UI 节点树、挂载脚本组件、拖拽面板变量赋值、调整动画帧等）。
   
2. **计划明细要求 (Plan Specification)**：
   在输出执行计划时，必须使用明确的列表将其强制拆分为两部分：
   - `[Agent 执行项]`：明确列出你要修改哪些纯代码文件。
   - `[User 待办项]`：明确列出需要用户在引擎编辑器中手动完成的配置动作。

3. **TODO 交接锚点**：
   对于任何需要用户在引擎编辑器中配合修改的地方（如暴露了新的序列化变量、预留了 UI 按钮的点击事件回调等），必须在对应的代码处添加醒目的 TODO 注释：
   `// TODO: [User Action] 请在编辑器 Inspector/属性面板 中完成此处引用的拖拽赋值或事件绑定`

# 📝 代码交付标准 (Code Standards)
- **完整注释强制令**：Agent 实现的所有新逻辑、状态机跳转、复杂的异步流处理，必须附加完整、清晰的中文注释，说明其设计意图与作用。拒绝交付没有注释的“黑盒”代码。