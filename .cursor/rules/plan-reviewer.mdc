---
description: 跨技术栈通用的 Plan 模式动态自省与重构规则 (Dynamic Agentic Planning Loop - Ultimate)
globs: *
alwaysApply: true
---

# 🧠 规划态动态 Agent Loop 强制协议 (Dynamic Planning Reflection Loop)

当你处于 Plan 模式且被要求输出开发计划时，必须在内部执行「起草 -> 找茬与重构的动态循环」，直到方案逻辑上完美无缺。

## ⚠️ 极简任务豁免权 (Trivial Task Bypass)
- 如果评估后发现用户的需求是纯文本修改、极其简单的 UI 调整（如改颜色/错别字）或无需考虑系统状态的原子级修改，**请直接跳过 The While-Loop**，直接输出 `📝 最终输出 (Final Plan)`。

## ⚙️ 前置执行工具集 (Pre-Planning Tools)
遇到复杂逻辑需求时，**必须在输出任何文本前**，先主动调用以下工具消除信息差：
1. **`search_codebase` / `read_file`**：读取基类、接口定义和被依赖模块。
2. **`grep_search`**：精准评估爆炸半径。

## 🔄 强制动态循环流 (The While-Loop)

除了被豁免的极简任务，你必须在回答的开头展示你的推演过程，格式如下：

### [Draft: 初始架构]
- 极简写出初步解决思路。**绝对禁止写出完整的代码实现**，只能使用自然语言大纲或极简伪代码。

### [Iteration 1: 漏洞注入与重构]
扮演极其苛刻的 QA 架构师，对当前方案发起致命提问。必须覆盖以下 4 个通用维度：
1. **状态机与生命周期**：网络断开、请求超时、组件/实例意外销毁时，会导致死锁、内存泄漏或空引用吗？
2. **时序与并发控制**：异步操作顺序颠倒，或连续重入，会导致数据脏写或竞态条件吗？
3. **边界与异常捕获**：空数据、极值、非预期输入、上游 API 报错，都有兜底处理吗？
4. **过度设计审查 (Scope Creep)**：为修复上述漏洞引入的补丁，是否过于笨重？能否用更简单优雅的方式解决？

- **本轮发现的漏洞**：[列出发现的问题，若完美无缺，直接跳至 Final Plan]
- **本轮补丁方案**：[只输出针对漏洞的极简修改思路，严禁大段写代码]

### [Iteration N: 继续找茬...] (根据需要重复，最多进行 3 轮)
- 继续审查上一轮的补丁。
- **退出条件**：确信当前方案已覆盖上述 4 个维度，或者已达到 3 轮 Iteration，立即停止循环。

---

## 📝 最终输出 (Final Plan)
跳出循环后，输出一份纯粹的、无懈可击的技术施工图纸：
- 🎯 **[Agent 执行项]**：具体要改哪些文件、加什么逻辑（Checklist 形式）。
- 👤 **[User 待办项]**：需要人类在编辑器、UI界面或环境中配合的操作。
- ⚠️ **[残余风险提示]**：如果在达到 3 轮最大迭代后，由于底层架构限制仍有无法完美解决的隐患，必须在此处向人类架构师明确指出。